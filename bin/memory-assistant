#!/bin/bash

THRESHOLD_NOTIFY=80
THRESHOLD_ALERT=88

get_mem_usage() {
  total=$(awk '/MemTotal:/ {print $2}' /proc/meminfo)
  available=$(awk '/MemAvailable:/ {print $2}' /proc/meminfo)

  if [[ $total -gt 0 ]]; then
    echo $(( (total - available) * 100 / total ))
  else
    echo 0
  fi
}

get_procs() {
  ps -eo comm,rss | awk 'NR>1 { mem[$1] += $2 }
    END { for (c in mem) print mem[c], c }' | \
    sort -k1 -rn | head -20 | awk '{
      kb=$1; sub(/^[0-9]+ /,""); name=$0
      gb=kb/1024/1024
      if(gb>=1) printf "%s\n%.2f GB\n", name, gb
      else { mb=kb/1024; printf "%s\n%.0f MB\n", name, mb }
    }'
}

show_notification() {
  local pct="$1"
  local title="Memory Alert"
  local body="Memory usage at ${pct}% — rising above ${THRESHOLD_NOTIFY}%"

  if command -v notify-send &> /dev/null; then
    notify-send "$title" "$body"
  else
    zenity --notification --text="$title: $body" 2>/dev/null
  fi
  canberra-gtk-play --id=dialog-warning &
}

show_alert() {
  local used_percent="$1"
  local procs
  procs=$(get_procs)

  choice=$(echo "$procs" | zenity --list \
    --title="⚠️ System memory critically high (${used_percent}%)" \
    --text="Memory has reached ${THRESHOLD_ALERT}% — force quit applications below.\n" \
    --column="Application" --column="Memory" \
    --width=800 --height=600 \
    --ok-label="Force Quit" \
    --cancel-label="Cancel" \
    --icon=dialog-warning)

  if [ -n "$choice" ]; then
    app=$(echo "$choice" | awk '{print $1}')

    LOG=""
    log() { LOG+="$1\n"; }

    log "═══════════════════════════════════════════"
    log " Force Quit"
    log " Target: $app"
    log "═══════════════════════════════════════════"

    zenity --progress --pulsate --no-cancel \
        --text="Force quitting $app..." \
        --title="Processing" --width=350 &
    LOADING_PID=$!

    KILLED=0

    log ""
    log " [1/3] pkill -9 -x \"$app\""
    if pkill -9 -x "$app" 2>/dev/null; then
      log " -> OK"
      KILLED=1
    else
      log " -> no process matched (exit 1)"
      log ""
      log " [2/3] pkill -9 \"$app\""
      if pkill -9 "$app" 2>/dev/null; then
        log " -> OK"
        KILLED=1
      else
        log " -> no process matched (exit 1)"
        log ""
        log " [3/3] pkill -9 -f \"$app\""
        if pkill -9 -f "$app" 2>/dev/null; then
          log " -> OK"
          KILLED=1
        else
          log " -> no process matched (exit 1)"
        fi
      fi
    fi

    kill "$LOADING_PID" 2>/dev/null
    wait "$LOADING_PID" 2>/dev/null

    log ""
    if [ "$KILLED" -eq 1 ]; then
      log " ✅ $app sent SIGKILL"
    else
      log " ⚠️ Could not kill $app"
    fi
    log "═══════════════════════════════════════════"

    echo -e "$LOG" | zenity --text-info \
        --title="Force Quit Result" \
        --width=500 --height=300 \
        --font="monospace"
  fi
}

LOCK_FILE="/tmp/plco-memory-assistant-alert.lock"

cleanup() {
  rm -f "$LOCK_FILE"
}
trap cleanup EXIT

# --- Main loop ---

PREV_USAGE=0
LAST_NOTIFY=0
NOTIFY_INTERVAL=300

while true; do
  usage=$(get_mem_usage)
  now=$(date +%s)

  if [ "$usage" -ge "$THRESHOLD_ALERT" ]; then
    if [ ! -f "$LOCK_FILE" ] || ! kill -0 "$(cat "$LOCK_FILE")" 2>/dev/null; then
      echo $$ > "$LOCK_FILE"
      show_alert "$usage"
      rm -f "$LOCK_FILE"
    fi
  elif [ "$usage" -ge "$THRESHOLD_NOTIFY" ] && \
       [ "$usage" -gt "$PREV_USAGE" ] && \
       [ $((now - LAST_NOTIFY)) -ge "$NOTIFY_INTERVAL" ]; then
    show_notification "$usage"
    LAST_NOTIFY="$now"
  fi

  PREV_USAGE="$usage"
  sleep 1
done
